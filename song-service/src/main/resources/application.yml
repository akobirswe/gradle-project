spring:
  application:
    name: song
    data:
      url: jdbc:postgresql://localhost:5432/song_db
      username: username
      password: password
      jpa:
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
          ddl-auto: update

    server:
      port: 8082
